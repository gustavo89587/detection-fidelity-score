DFS in Detection Pipelines

DFS can be used to evaluate detection policy changes before deployment.

Example workflow:

1. Engineer submits new detection rule
2. DFS evaluates rule against dataset
3. DFS compares with baseline
4. Impact report is generated
5. SOC decides to deploy or revise