## Objective
Generate PowerShell process with-enc and primitive download.

## Execution (safe)
1) run PowerShell with encoded command containing IWR.
2) Use a test URL (can be a local/controlled endpoint).

## Requirement
- Sysmon EventID 1 or Security 4688 with CommandLine enabled.
